<h1>Courses List</h1>

<%= button_to "Add New Course", new_course_path, method: :get %>
<ul>
    <% @courses.each do |course| %>
        <li><%= course.name%></li>
    <%end%>
</ul>

<%= button_to "Back to Students List", students_path, method: :get %>

